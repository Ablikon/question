# 💜 Altyn Question - Опросник для девушки

Красивый опросник в стиле 16 personalities для понимания чувств и эмоций.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка базы данных (локально)

Убедись, что PostgreSQL запущен (видно на скриншоте что уже работает):

```bash
# Настроить базу данных
npm run setup-db
```

Или вручную:
```bash
# Создать базу данных
createdb altyn_survey

# Выполнить SQL скрипт
psql -d altyn_survey -f init-db.sql
```

### 3. Запуск в режиме разработки

```bash
npm run dev
```

Открой: http://localhost:5173

## 📱 Страницы

- `/` - Главная страница с опросником
- `/thank-you` - Страница благодарности
- `/admin-secret-panel-2024` - Админ-панель (пароль: `altyn2024`)

## 🌐 Деплой на Vercel

### 1. Создать базу данных в Neon

1. Зайди на [neon.tech](https://neon.tech)
2. Создай новый проект
3. Скопируй connection string

### 2. Деплой

```bash
# Установи Vercel CLI (если нет)
npm i -g vercel

# Деплой
vercel
```

### 3. Добавить переменные окружения в Vercel

1. Зайди в настройки проекта на vercel.com
2. Settings → Environment Variables
3. Добавь: `DATABASE_URL` = твой Neon connection string
4. Redeploy проект

### 4. Выполнить SQL в Neon

Зайди в Neon SQL Editor и выполни содержимое `init-db.sql`:

```sql
CREATE TABLE IF NOT EXISTS survey_responses (
  id SERIAL PRIMARY KEY,
  answers JSONB NOT NULL,
  submitted_at TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_submitted_at ON survey_responses(submitted_at DESC);
```

## 🎨 Особенности

- ✨ Красивый дизайн в стиле 16 personalities
- 📱 Адаптивный под iPhone 14 Pro Max
- 🎭 Плавные анимации с Framer Motion
- 📊 Диаграммы и статистика в админке
- 💾 Сохранение в PostgreSQL
- 🔒 Защищенная админ-панель

## 🛠️ Технологии

- React 19
- Vite
- React Router
- Framer Motion
- Recharts
- Axios
- PostgreSQL
- Vercel Serverless Functions

## 💜 Для тебя, солнышко

Этот опросник создан с любовью, чтобы лучше понять тебя и твои чувства.
# question
